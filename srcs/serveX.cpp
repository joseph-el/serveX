# include "serveX.hpp"

int serveX(int argc, char *argv[])
{
    try {
        // grammar of config file
        // syntax
    
    }
    catch (std::exception &e) {
        //  (for abderahman ) u have to set a std::execption like in ex00 / cpp module 5
        std::cout << e.what() << std::endl;
    
    }

    exit (EXIT_SUCCESS);
}